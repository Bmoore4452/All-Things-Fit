{{!-- list of custom workouts --}}
<div class = "card-container">
    {{#each serializedData as |data|}}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{data.id}}</h5>
            <p class="card-text">{{data.}}</p>
        </div>
    </div>
    {{/each}}
</div>

{{!-- specific routines w/in custom workout --}}
<div class = "card-container">
    {{#each }}
    
    {{/each}}
</div>